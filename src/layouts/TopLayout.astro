---
import TopTagList from '@components/top/TopTagList.astro';
import TopBanner from '@components/top/TopBanner.astro';

interface Props {
  count: number;
}

const { count } = Astro.props;
---

<TopBanner />
<h1 class="mb-3 text-xl font-bold text-gray-600">ボタン一覧（{count}個）</h1>
<TopTagList />
<slot />
